# This workflow will build a Java project with Ant
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-ant

name: Java CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

#jobs:
  #build:
  
jobs:
  run_tests:
    runs-on: ubuntu-20.04
    steps:
    - uses: actions/checkout@v2
    - name: Run script file
      run: |
         chmod +x ./scripts/regenerate_apriltag_files.sh
         ./scripts/regenerate_apriltag_files.sh
      shell: bash

    #runs-on: ubuntu-latest

    #steps:
    #- uses: actions/checkout@v3
    #- name: Set up JDK 11
    #  uses: actions/setup-java@v3
    #  with:
    #    java-version: '11'
    #    distribution: 'temurin'
    #- name: Build with Ant
    #  run: |
    #    ant
    #    java -cp april.jar april.tag.GenerateTags april.tag.TagStandard41h7.java

#java -cp april.jar april.tag.GenerateTags april.tag.TagStandard41h7

#java -cp april.jar april.tag.TagFamilyGenerator standard_9 7
